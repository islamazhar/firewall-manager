// PascalCasing
function Message() {
    // JSX: JavaScript XML
    // Go to babeljs.io to see how it gets compiled to JS
    const name = 'Mosh';
    if (name)
        return <h1>Hello {name}</h1>;
    else 
    return <h1>Hello Word</h1>;
}

export default Message;